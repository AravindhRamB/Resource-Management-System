// import { RadioGroupProps} from "@/module/formFields";
export function RadioGroup({  }) {
    // return (
    //     <div className="m-1 p-1">
    //         {options.map((option) => (
    //             <div key={option.value} className="flex items-center mb-2">
    //                 <input
    //                     type="radio"
    //                     id={option.value}
    //                     name={name}
    //                     value={option.value}
    //                     onChange={() => onChange?.(option.value)}
    //                     className="mr-2 h-4 w-4 border-gray-300 rounded focus:ring-blue-500 focus:ring-2"
    //                 />
    //                 <label htmlFor={option.value} className="text-base font-medium">
    //                     {option.label}
    //                 </label>
    //             </div>
    //         ))}
    //     </div>
    // );
}